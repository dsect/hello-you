{
  "name": "Hello You Web App",
  "image": "mcr.microsoft.com/devcontainers/typescript-node:1-22-bookworm",
  "features": {
    "ghcr.io/devcontainers/features/docker-in-docker:2": {}
  },
  "containerEnv": {},
  "postCreateCommand": "npm install supabase && cd apps/web && npm install && ([ -f supabase/config.toml ] || npx supabase init --yes) && npx playwright install-deps",
  "postStartCommand": "npx supabase stop 2>/dev/null || true && npx supabase start",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.vscode-github-actions"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  }
}
